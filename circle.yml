machine:
  python:
    version: 3.5.0
  java:
    version: openjdk8

dependencies:
  pre:
      - make all
  cache_directories:
    - "build/cmake-3.5.2"
    - "build/seqan-seqan-v2.1.1"
    - "build/python/whatshap"
